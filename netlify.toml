[build]
  command = "cd frontend && npm install --legacy-peer-deps && npm install ajv@8 ajv-keywords@5 --legacy-peer-deps && CI=false npm run build"
  publish = "frontend/build"
  functions = "netlify/functions"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
